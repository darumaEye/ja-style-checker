"""
# 送り仮名（要領 Ⅰ-2）

送り仮名に関する機械判定。

## 未実装

- 現時点では実装済みのチェックはない。`koyobun_rules.md`の該当項目
  （活用のない複合語186語の送り仮名省略、動詞の送り仮名等）は
  辞書化されていないため、将来186語辞書を追加する場合はこのモジュールに実装する。
"""


def run(text, findings):
    """このカテゴリの全チェックを実行する（現状は何もしない）。"""
    pass
